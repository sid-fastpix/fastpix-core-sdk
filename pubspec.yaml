name: flutter_core_sdk
description: Flutter Core SDK for player events
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  device_info_plus: ^11.4.0
  ffmpeg_kit_flutter_new: ^1.5.0
  flutter:
    sdk: flutter
  http: ^1.4.0
  uuid: ^4.5.1
  logging: ^1.2.0
  # path: ^1.8.0
  synchronized: ^3.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
